import { Metadata } from "next";
import RoadMapPage from "./page";

export const metadata: Metadata = {
    title: "Roadmap Items | Product Feedback App",
    description: "Roadmap Items| Product Feedback App"
};

export default RoadMapPage;